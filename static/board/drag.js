// Standalone drag-and-drop helpers extracted from board.js.
// Session 14 intentionally keeps this file minimal because the current
// drag behavior still lives primarily on the _createBoard()
// controller and depends on `this`.
