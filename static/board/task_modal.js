// Standalone task modal helpers extracted from board.js.
// Session 15 intentionally keeps this file minimal because the current
// modal rendering and behavior still live primarily on the _createBoard()
// controller and depend on `this`.
