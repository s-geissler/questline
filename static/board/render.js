// Standalone DOM/render helpers extracted from board.js.
// Session 14 intentionally keeps this file minimal because the current
// board rendering logic still lives primarily on the _createBoard()
// controller and depends on `this`.
